function maxAverageSum(nums: number[], k: number): number {
    let result = [], windowSum = 0

    for (let right = 0; right < nums.length; right++) {
        windowSum += nums[right]

    }

    return 0
}